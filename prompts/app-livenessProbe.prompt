Generate a **Pod** named `app-livenessprob` in namespace `demo` that runs one container **app** on image `gcr.io/smartcity-gl/demo:v1.0.1`. Expose container port **8080** (`name: http`). Configure an HTTP **livenessProbe** hitting `/` on port **8000** with `initialDelaySeconds: 5`, `timeoutSeconds: 1`, `periodSeconds: 10`, `failureThreshold: 3`. Return only YAML. 